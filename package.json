{
  "name": "second-life-commerce",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered Marketplace Security — root dev orchestration (Phase 3.5)",
  "scripts": {
    "dev": "concurrently -n backend,frontend,ml -c blue,green,magenta \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ml\"",
    "dev:web": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:ml": "cd ml-service && python -m uvicorn app.main:app --reload --port 8000",
    "seed": "npm --prefix backend run seed",
    "seed:trust": "npm --prefix backend run seed:trust",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
