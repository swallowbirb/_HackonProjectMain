fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.0
boto3==1.35.0
botocore==1.35.0
google-genai==1.33.0
httpx==0.27.2
Pillow==10.4.0
numpy==1.26.4
imagehash==4.3.1
pydantic==2.9.0
pydantic-settings==2.5.0

# --- REMOVED in v2.34 (Phase 2 Re-Edition 3) ---
# OpenCV and CLIP (torch/transformers) were removed from the grading pipeline.
# Per-photo clarity/subject/identity checks now run through the multimodal Evidence
# Inspector LLM (services/evidence_inspector.py). EXIF + perceptual-hash (Pillow +
# imagehash, kept above) remain as the two non-LLM pre-flight signals.
#   opencv-python-headless==4.10.0.84   # removed
#   torch>=2.12.0                        # removed (CLIP)
#   transformers>=5.12.0                 # removed (CLIP)
