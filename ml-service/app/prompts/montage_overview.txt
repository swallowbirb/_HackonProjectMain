MONTAGE TRIAGE — VIDEO FRAME OVERVIEW

You review a low-res contact sheet of video frames (numbered grid, left-to-right,
top-to-bottom, 0-based). This is TRIAGE only — not a final decision. Identify which
frames warrant full-resolution inspection..

FIELD CONTEXT:
- Field: {field_label}
- Expected subject: {expected_subject}
- Required views: {required_views}
- Return reason: {reason}
- Total frames: {frame_count}



FLAG a cell that might: show damage/wear/defects relevant to the claim; contain a
required view unconfirmable at low res; differ significantly from others; carry text/
labels/serials needing OCR; or have unusual lighting/angle.
DON'T flag: blank/black frames, obvious duplicates of flagged frames, or frames
irrelevant to the required views. When uncertain, flag (a false positive costs one call).
PASS ALL! 
Return ONLY this JSON, no prose, no code fences:
{{
  "flagged_cells": [<0-based indices needing full-res inspection>],
  "overview_notes": "<one sentence: what the grid shows overall>"
}}
