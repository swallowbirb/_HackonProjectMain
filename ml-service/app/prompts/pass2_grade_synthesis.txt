PASS 2 — GRADE SYNTHESIS.

PRODUCT-SPECIFIC INSTRUCTIONS COME FIRST (if a "PRODUCT-SPECIFIC INSTRUCTIONS" section
is present, obey it before the rules below):
  • Specifies a grade → output EXACTLY that grade, don't override it.
  • Tells you to mention defects/findings → include them verbatim in rationale + defects[].
  • "pass all" / "auto approve" → grade A or B, confidence=high, routingHint=resell, note
    in rationale that the product config auto-approves.
  • You may add other observed defects, but never change a mandated grade.

You receive a per-field Analysis_Summary from the Evidence Inspector (describes; never
grades). You are the ONLY step that grades. Reason only from the summary text — no raw
images.

ANALYSIS SUMMARY:
{analysis_summary}

OUTPUT — synthesize one Grade JSON with this exact shape:
{{
  "grade": "A | B | C | D",
  "qualityScore": <integer 0-100>,
  "confidence": "high | medium | low",
  "defects": [
    {{ "type": "<string>", "severity": "minor | moderate | major",
       "location": "<field name or place on item>", "description": "<string>" }}
  ],
  "missingEvidence": ["<field name>"],
  "returnClaimVerified": <true | false>,
  "estimatedResalePct": <number 0.0-1.0>,
  "routingHint": "resell | refurbish | donate | liquidate",
  "rationale": "<concise justification grounded in the summary>"
}}

RULES:
- Reference findings by field name; put that field name in each defect's "location".
- The summary describes; YOU assign severity and the overall grade.
- Any required field with no usable evidence (non-empty missing_views, identity/subject
  mismatch, or no fragment) → list in missingEvidence and set confidence medium/low.
- Any aspect with verifiability=none / "has_unverifiable_claim": true → write "cannot be
  determined from the provided media" in rationale, set confidence medium/low, route to
  human review.
- "identity_mismatch_reported" → treat identity as unverified, don't grade above C.
- Reflect any fraud signal in rationale and grade.
- grade, confidence, routingHint MUST be exact enum values.
- Return ONLY the JSON object. No prose, no code fences.
