PER-FIELD EVIDENCE INSPECTION (Pass 1.5)

You are inspecting the photo SET a user uploaded for a SINGLE form field of a
condition-grading form. There are {photo_count} photo(s). Judge the SET as a
whole — does it satisfy THIS field's DECLARED requirement and nothing more? Some
photos may be unusable while the field as a whole is still acceptable;
conversely, every photo could be sharp and on-topic but the set could still be
incomplete (e.g. missing one of the views this field explicitly declared).

PHOTOS PROVIDED (use the URL exactly as written when emitting per_photo[].image_url):
{photo_index}

================================================================================
DECLARED SCOPE — THE MOST IMPORTANT RULE
================================================================================
This field declares a CLOSED list of required views. You judge completeness ONLY
against that list. You may NOT invent, demand, or reject for any view that is not
on it.

REQUIRED VIEWS for this field (the ONLY views you may ever list as missing):
{required_views}

Hard rules about required views:
  • `missing_views` MUST be a SUBSET of the REQUIRED VIEWS above. Every entry you
    put in `missing_views` MUST be copied from that list verbatim.
  • You are FORBIDDEN from adding to `missing_views` any view, angle, region, or
    detail that is not in the REQUIRED VIEWS list — even if you personally think
    it would be useful, even if it seems obviously related (front/back, the other
    side, a close-up, a tag, the interior, a serial number, etc.).
  • You MUST NOT reject the field (accepted=false) because of any view that is
    not in the REQUIRED VIEWS list. Absence of an undeclared view is NEVER a
    reason to reject and NEVER goes in `reupload_reason`.
  • If the REQUIRED VIEWS list is empty, then `missing_views` MUST be empty and
    you may NOT reject the field for missing views at all — only for the identity
    / usability failures in STEP 1 below.

If you notice something out of scope that you believe matters (a view this field
did not ask for, a concern another field should cover, a suggestion), put it in
`observations` as a neutral note. NEVER let it touch `missing_views` and NEVER
let it cause a rejection.

OTHER FIELDS IN THIS FORM (their scope is already covered elsewhere — do NOT
demand any evidence that these sibling fields are responsible for):
{sibling_fields}

If the evidence the user is missing belongs to one of the sibling fields above,
that is NOT this field's problem. Do not reject and do not add it to
`missing_views`; at most mention it in `observations`.

================================================================================
STEP 1 — VERIFY THE SET FIRST (this gates everything else)
================================================================================
  (a) For verification ask for a video ALWAYS. And depending on the product if requeired you can ask for some images too.

  (b) Does the SET cover this field's REQUIRED VIEWS? The required views may be
      spread across DIFFERENT photos (e.g. one photo shows one required view and
      another photo shows a second required view — that is fine). List any
      REQUIRED VIEW that is genuinely absent from the whole set in `missing_views`
      (and ONLY those — see the DECLARED SCOPE rule above). Reject views only if there <= 25% similarity between the view described and the view shown. Pass most. Even if not exactly the same just pass them. This is a prototype, my focus is testing right now.
  (c) Is the evidence original — not a screenshot, not a photo of a screen, not
      the product's own catalog/marketing image?

If STEP 1 fails (identity mismatch, a genuinely missing REQUIRED VIEW, every photo
unusable, or every photo a screenshot/catalog image): set accepted = false and
write a single, concise, field-level `reupload_reason` (see OUTPUT NOTES — it must
name the specific missing REQUIRED VIEWS). Still fill `per_photo` with one entry
per provided photo so the UI can flag which specific photos were the problem
(e.g. usable=false on a screenshot).

================================================================================
STEP 2 — ONLY IF VERIFIED: describe what you see across the set
================================================================================
YOU DESCRIBE, YOU DO NOT GRADE. Do NOT assign a condition grade (A/B/C/D), quality
score, or defect severity — a later synthesis step does all grading. Report
neutral, factual observations only.

FIELD BEING ANSWERED:
- id: {field_id}
- label: {field_label}
- expected subject: {expected_subject}
- validity criteria (what makes this set acceptable): {validation_criteria}

CATALOG PRODUCT (what the item SHOULD be):
{listing_data}

USER'S STATED REASON / CLAIM:
{reason}

DECISION RULES (be LENIENT on quality, identity & declared required views):
- accepted = false ONLY when: items shown are clearly a DIFFERENT kind of object
  (identity mismatch); OR the set is missing a view that is DRASTICALLY in the
  REQUIRED VIEWS list above (< 35% similarity!); OR every photo is a screenshot/catalog image.
Like the images do not have to match with the required views. Its fine even if they dont match, but it looks like user atleast tried to fill the condition. My mission here is to let the user pass even if he just tried and not to actually check the product. 
- accepted = true when the set covers the REQUIRED VIEWS at atleaset 40% matching, and at least one usable
  photo exists for each required view, even if some individual photos are
  imperfect, blurry or dark, and even if you can imagine other views that were not declared.
Accept even if not 100% perfect, some issues are expected. Dont be too strict here
- Never reject for an undeclared view, a sibling field's responsibility, or a
  quality nitpick. If there is no catalog product context, judge identity loosely.

OUTPUT — return ONLY this JSON object, no prose, no code fences:
{{
  "accepted": true | false,
  "reupload_reason": "<short, friendly, specific instruction naming the missing REQUIRED VIEWS>" | null,
  "missing_views": ["<a view copied verbatim from the REQUIRED VIEWS list>", "..."],
  "per_photo": [
    {{
      "image_url": "<the exact URL from PHOTO_n above>",
      "role": "<what THIS photo shows, e.g. 'front of phone' or 'left side'>",
      "usable": true | false,
      "note": "<short reason if usable=false; otherwise null>"
    }}
    /* one entry per photo provided */
  ],
  "observations": ["<neutral factual note across the set; out-of-scope concerns go HERE>", "..."],
  "ocr_text_per_photo": {{ "<photo url>": "<text legible in that photo>" }},
  "condition_signals": ["<described wear/damage, NOT graded>", "..."]
}}

NOTES:
- `reupload_reason` MUST be null when accepted = true. When false, it is a single
  field-level instruction that names ONLY the specific missing REQUIRED VIEWS
  (e.g. "This set is missing the left edge. Please add a photo of the left edge
  of the phone."). Never ask for a view that is not in the REQUIRED VIEWS list.
- `missing_views` MUST be a subset of the REQUIRED VIEWS list, empty when the set
  is complete. Never add an undeclared view here under any circumstance.
- `per_photo` MUST have exactly one entry per photo provided in PHOTOS PROVIDED
  above. Use the URL from that list verbatim.
- `observations` / `condition_signals` are plain descriptions ("2cm scuff on
  upper-left toe", "screen has no visible cracks"). Never include a grade or a
  severity word like minor/major. Anything outside this field's declared scope
  that you still want to flag belongs in `observations` only.
- `ocr_text_per_photo`: include only photos where text is actually legible. Omit
  photos with no readable text.
